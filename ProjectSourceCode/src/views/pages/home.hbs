<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>What's Going On?</title>

    <style>
        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
            overflow: hidden;
            font-family: Arial, sans-serif;
        }

        /* Video Background */
        .background-video {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
            z-index: -1; /* Behind all other content */
        }

        h1 {
            margin-top: 30px;
            text-align: center;
            font-family: 'Arial Black', sans-serif;
            font-size: 48px;
            font-weight: bold;
        }

        p {
            text-align: center;
            font-family: 'Courier New', monospace;
            font-size: 18px;
        }

        /* Additional styling for other elements... */
    </style>
</head>

<body>
    <!-- Video Background -->
    <video class="background-video" autoplay loop muted>
        <source src="ProjectSourceCode/src/resources/img/Flying Over CU-Boulder.mp4" type="video/mp4">
        Your browser does not support the video tag.
    </video>

    <h1><b>What's Going On?</b></h1>
    <p>Here are all the events currently taking place:</p>
    <div class="container text-center">
        <div class="row align-items-start">
            {{#if events.length}}
            {{#each events}}
            <div class="col-md-4">
                <div class="hobby-card-{{@index}}">
                    <div class="card-body">
                        <h5 class="card-title">{{event_name}}</h5>
                        <p class="card-text">{{event_date}}</p>
                        <p class="card-text">{{event_location}}</p>
                        <p class="card-text">{{event_description}}</p>
                    </div>
                </div>
            </div>
            {{/each}}
            {{else}}
            <div class="col-12">
                <p>No events found. Please check back later.</p>
            </div>
            {{/if}}
        </div>
    </div>
    <a href="/map">Try out the map</a>
</body>

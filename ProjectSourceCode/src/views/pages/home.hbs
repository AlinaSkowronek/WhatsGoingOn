<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>What's Going On?</title>

    <style>
        body,
        html {
            margin: 0;
            padding: 0;
            height: 100%;
            font-family: Arial, sans-serif;
            background-color: rgb(207, 184, 124);
            overflow: hidden;
        }

        /* Title Section */
        .title-section {
            text-align: center;
            font-family: 'Arial Black', sans-serif;
            font-size: 48px;
            font-weight: bold;
            margin-top: 10px;
            z-index: 2;
            position: relative;
        }

        h1 {
            margin-top: 1px;
            text-align: center;
            font-family: 'Arial Black', sans-serif;
            font-size: 48px;
            font-weight: bold;
        }

        p {
            text-align: center;
            font-family: 'Courier New', monospace;
            font-size: 18px;
        }

        /* Background Video */
        .bg-video {
            position: fixed;
            top: 51%;
            left: 50%;

            transform: translate(-50%, -50%);
            min-width: 50%;
            min-height: 50%;
            width: 60vw;
            /* Adjusted size */
            height: auto;
            z-index: 1;
            object-fit: cover;
            border: 7px, black solid;
        }


        /* Map Link Section */
        .map-link-section {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            text-align: center;
            font-family: 'Courier New', monospace;
            z-index: 2;
        }

        .map-link {
            font-size: 28px;
            color: #000000;
            font-weight: bold;
            text-decoration: none;
            padding: 10px 20px;
            background-color: #EBEBEB;
            border: 3px solid;
            border-radius: 8px;
        }

        /* Event List Section - Minimized with hover to expand */
        .event-list-container {
            position: absolute;
            bottom: 3%;
            left: 50%;
            transform: translateX(-50%);
            width: 90%;
            text-align: center;
            z-index: 2;
        }

        .event-list-title {
            font-size: 18px;
            color: white;
            background-color: rgba(0, 0, 0, 0.3);
            padding: 10px;
            border-radius: 8px;
            cursor: pointer;
            display: inline-block;
            position: relative;
            top: -15px;
        }

        /* Hidden event list initially */
        .event-list {
            display: none;
            max-height: 400px;
            overflow-y: auto;
            margin-top: 10px;
            background-color: rgba(0, 0, 0, 0.3);
            color: white;
            padding: 20px;
            border-radius: 8px;
        }

        /* Show events to hover over the title */
        .event-list-container:hover .event-list {
            display: block;
        }

        .card {
            background-color: #fff;
            color: #333;
            border-radius: 8px;
            padding: 15px;
            margin: 10px 0;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        .card h5 {
            font-size: 20px;
            margin-bottom: 10px;
        }

        .card p {
            font-size: 14px;
            margin: 5px 0;
        }
    </style>
</head>

<body>

    <!-- Background Video -->
    <video class="bg-video" autoplay muted loop>
        <source src="../../resources/img/Flying Over CU-Boulder.mp4" type="video/mp4">
        Your browser does not support the video tag.
    </video>

    <!-- Title Section -->
    <div class="title-section">
        <h1><b>What's Going On?</b></h1>
    </div>

    <!-- Map Link Section -->
    <div class="map-link-section">
        <a href="/map" class="map-link">Try out the map!</a>
    </div>

    <!-- Event List Section -->
    <div class="event-list-container">
        <div class="event-list-title">Here are all the events currently taking place</div>
        <div class="event-list">
            <div class="container text-center">
                <div class="row align-items-start">
                    {{#if events.length}}
                    {{#each events}}
                    <div class="card">
                        <h5 class="card-title">{{event_name}}</h5>
                        <p class="card-text">{{event_date}}</p>
                        <p class="card-text">{{event_location}}</p>
                        <p class="card-text">{{event_description}}</p>
                    </div>
                    {{/each}}
                    {{else}}
                    <p>No events found. Please check back later.</p>
                    {{/if}}
                </div>
            </div>
        </div>
    </div>

</body>

</html>
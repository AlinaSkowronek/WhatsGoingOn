<p>This is the home page. This is where users will be redirected after login!</p>
<p>Here are all the events currently taking place:</p>
<div class="container text-center">
  <div class="row align-items-start">
    {{#if events}}
    {{#each events}}
    <div class="col-md-4">
      <div class="hobby-card-{{@index}}">
        <div class="card-body">
          <h5 class="card-title">{{eventname}}</h5>
          <p class="card-text">{{eventtime}}</p>
          <p class="card-text">{{eventlongitude}}</p>
          <p class="card-text">{{eventlattitude}}</p>
        </div>
      </div>
    </div>
    {{/each}}
    {{else}}
    <div class="col-12">
      <p>No events found. Please check back later.</p>
    </div>
    {{/if}}
  </div>
</div>
<a href="/map">Try out the map</a>